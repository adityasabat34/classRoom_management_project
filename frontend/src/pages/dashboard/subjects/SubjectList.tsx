import React from "react";

const SubjectList = () => {
  return (
    <div>
      SubjectList - Used when: User opens Subjects from sidebar Contains: Search
      bar Filters Subjects table Uses: useSubjects() hook DataTable component
    </div>
  );
};

export default SubjectList;
